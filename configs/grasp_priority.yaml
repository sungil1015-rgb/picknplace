priority:
  # 우선순위는 현재 3개 값만 본다:
  # 1) 파지점 주변 local depth
  # 2) classification similarity
  # 3) classification reject reason
  #
  # 실제 정렬 점수(total)는 아직 local depth score만 사용하고,
  # similarity/reject reason은 debug와 다음 정책 결정을 위해 저장한다.
  local_depth_window: 11
