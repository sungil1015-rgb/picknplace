model:
  checkpoint: facebook/mask2former-swin-large-coco-instance
  weights: weights/mask2former/model.pt
input:
  size: [650, 980]
crop:
  enabled: true
  tolerance: 0.05
postprocess:
  prediction_threshold: 0.8
  mask_threshold: 0.6
  overlap_threshold: 0.4
  min_instance_area: 1000.0
